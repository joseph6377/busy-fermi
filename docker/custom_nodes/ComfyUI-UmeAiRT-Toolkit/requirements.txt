# Core dependencies
colorama
opencv-python
tqdm
scipy
psutil
safetensors
# GPU Monitoring (NVIDIA — graceful degradation if missing)
pynvml; sys_platform == 'win32' or (sys_platform == 'linux' and platform_machine != 'aarch64')
pyamdgpuinfo; sys_platform == 'linux'
# SeedVR2 upscaler (vendored)
omegaconf>=2.3.0
diffusers>=0.33.1
peft>=0.17.0
rotary_embedding_torch>=0.5.3
einops
# Face Detailer
ultralytics>=8.0.0
# GGUF model support
gguf
# Image Analysis (Florence2 VLM)
transformers>=4.38.0
tokenizers
requests>=2.33.1
